<Page
    x:Class="TypingTrainer.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TypingTrainer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBox x:Name="username" PlaceholderText="Username" Margin="25" FontSize="50" Width="400"/>
            <PasswordBox x:Name="password" PlaceholderText="Password" Margin="25" FontSize="50" Width="400"/>
            <Button x:Name="submitBtn" Click="submitBtn_Click" Content="Submit" HorizontalAlignment="Center" Margin="50" FontSize="75" Padding="50, 15, 50, 15"/>
        </StackPanel>
    </Grid>
</Page>
