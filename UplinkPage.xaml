<Page
    x:Class="TypingTrainer.UplinkPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TypingTrainer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="6*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="5*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Button x:Name="UplinkPageBackBtn" Click="UplinkPageBackBtn_Click" Grid.Row="1" VerticalAlignment="Stretch" HorizontalAlignment="Left" Width="40"/>
        <Button x:Name="UplinkPageCreateBtn" Click="UplinkPageCreateBtn_Click" Grid.Row="0" Grid.Column="1" VerticalAlignment="Stretch" HorizontalAlignment="Center" Content="Create" FontSize="40" Padding="50, 0, 50, 0" Margin="10"/>
        <ScrollViewer Grid.Row="1" Grid.Column="1" Height="500">
            <StackPanel Orientation="Vertical" x:Name="UplinkPageBookDisplay"/>
        </ScrollViewer>
    </Grid>
</Page>
